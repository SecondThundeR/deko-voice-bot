services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file: .env
    environment:
      API_HOST: 0.0.0.0
      API_PORT: 3003
    healthcheck:
      test:
        - CMD
        - node
        - --input-type=module
        - --eval
        - const response = await fetch("http://127.0.0.1:3003/"); if (!response.ok) process.exit(1)
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s
    restart: unless-stopped

  bot:
    build:
      context: .
      dockerfile: apps/bot/Dockerfile
    env_file: .env
    environment:
      API_URL: http://api:3003
      SERVER_HOST: 0.0.0.0
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "${SERVER_PORT:-3002}:${SERVER_PORT:-3002}"
    restart: unless-stopped
